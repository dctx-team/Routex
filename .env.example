# Routex Environment Variables
# Routex 环境变量配置示例

# ============================================================================
# Server Configuration / 服务器配置
# ============================================================================

# Server port / 服务器端口
PORT=3000

# Server host / 服务器主机
HOST=0.0.0.0

# Node environment / Node 环境
NODE_ENV=production

# ============================================================================
# Security Configuration / 安全配置
# ============================================================================

# Master password for encryption (min 32 characters recommended)
# 加密主密码（建议至少 32 个字符）
MASTER_PASSWORD=your-super-strong-master-password-min-32-chars

# Encryption salt (64 hex characters, generate with: openssl rand -hex 32)
# 加密盐值（64 个十六进制字符，生成命令：openssl rand -hex 32）
ENCRYPTION_SALT=

# Signature secret for HMAC (min 32 characters recommended)
# HMAC 签名密钥（建议至少 32 个字符）
SIGNATURE_SECRET=your-signature-secret-for-request-verification

# ============================================================================
# Database Configuration / 数据库配置
# ============================================================================

# Database file path / 数据库文件路径
DATABASE_PATH=./data/routex.db

# Enable database encryption / 启用数据库加密
DATABASE_ENCRYPTION=false

# ============================================================================
# Logging Configuration / 日志配置
# ============================================================================

# Log level: debug, info, warn, error / 日志级别
LOG_LEVEL=info

# Log sensitive data (set to false in production) / 记录敏感数据（生产环境设为 false）
LOG_SENSITIVE_DATA=false

# ============================================================================
# CORS Configuration / 跨域配置
# ============================================================================

# Allowed origins (comma-separated) / 允许的来源（逗号分隔）
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ============================================================================
# Rate Limiting / 速率限制
# ============================================================================

# Enable rate limiting / 启用速率限制
RATE_LIMIT_ENABLED=true

# Default rate limit (requests per minute) / 默认速率限制（每分钟请求数）
RATE_LIMIT_MAX=100

# Rate limit window (milliseconds) / 速率限制窗口（毫秒）
RATE_LIMIT_WINDOW=60000

# ============================================================================
# Proxy Configuration / 代理配置
# ============================================================================

# Default timeout for proxy requests (milliseconds) / 代理请求默认超时时间（毫秒）
PROXY_TIMEOUT=300000

# Max retry attempts for failed requests / 失败请求最大重试次数
PROXY_MAX_RETRIES=3

# ============================================================================
# Cache Configuration / 缓存配置
# ============================================================================

# Cache warmup interval (milliseconds, 0 to disable) / 缓存预热间隔（毫秒，0 表示禁用）
CACHE_WARMUP_INTERVAL=300000

# ============================================================================
# Metrics & Monitoring / 指标和监控
# ============================================================================

# Enable Prometheus metrics / 启用 Prometheus 指标
METRICS_ENABLED=true

# Prometheus metrics path / Prometheus 指标路径
METRICS_PATH=/metrics

# Enable tracing / 启用追踪
TRACING_ENABLED=true

# Max spans to keep in memory / 内存中保留的最大 span 数量
TRACING_MAX_SPANS=10000

# ============================================================================
# Internationalization / 国际化
# ============================================================================

# Default locale / 默认语言
LOCALE=en

# Available locales / 可用语言
AVAILABLE_LOCALES=en,zh-CN

# ============================================================================
# Development / 开发环境
# ============================================================================

# Enable debug mode / 启用调试模式
DEBUG=false

# Pretty print JSON responses / 美化 JSON 响应
PRETTY_JSON=false

# ============================================================================
# OAuth Configuration / OAuth 配置 (可选)
# ============================================================================

# OAuth 认证配置，用于连接官方提供商账户
# OAuth configuration for connecting official provider accounts

# Anthropic OAuth / Anthropic OAuth 配置
# ANTHROPIC_OAUTH_CLIENT_ID=your_anthropic_client_id
# ANTHROPIC_OAUTH_CLIENT_SECRET=your_anthropic_client_secret
# ANTHROPIC_OAUTH_REDIRECT_URI=http://localhost:3000/api/oauth/callback

# OpenAI OAuth / OpenAI OAuth 配置
# OPENAI_OAUTH_CLIENT_ID=your_openai_client_id
# OPENAI_OAUTH_CLIENT_SECRET=your_openai_client_secret
# OPENAI_OAUTH_REDIRECT_URI=http://localhost:3000/api/oauth/callback

# Google OAuth (for Gemini API) / Google OAuth 配置（用于 Gemini API）
# GOOGLE_OAUTH_CLIENT_ID=your_google_client_id
# GOOGLE_OAUTH_CLIENT_SECRET=your_google_client_secret
# GOOGLE_OAUTH_REDIRECT_URI=http://localhost:3000/api/oauth/callback
